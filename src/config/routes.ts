const urls = {
  HOME: '/',
  DASHBOARD: '/dashboard',
  BLOG: '/blog',
  DOCS: '/docs',
  SETTINGS: '/settings',
  PROFILE: '/profile',
};

const routes = {
  HOME: urls.HOME,
  DASHBOARD: urls.DASHBOARD,
  BLOG: urls.BLOG,
  DOCS: urls.DOCS,
  SETTINGS: urls.SETTINGS,
  PROFILE: urls.PROFILE,
};

export default routes;
