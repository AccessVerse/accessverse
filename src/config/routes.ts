const urls = {
  HOME: '/',
  DASHBOARD: '/dashboard',
  BLOG: '/blog',
  DOCS: '/docs',
};

const routes = {
  HOME: urls.HOME,
  DASHBOARD: urls.DASHBOARD,
  BLOG: urls.BLOG,
  DOCS: urls.DOCS,
};

export default routes;
